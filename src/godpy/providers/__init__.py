"""Model-provider integrations that aren't covered by ADK natively or LiteLLM.

Currently: :mod:`godpy.providers.openai_chatgpt` — "Sign in with ChatGPT" (Codex OAuth),
which runs God on a ChatGPT subscription via the Responses backend.
"""
