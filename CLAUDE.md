# Gaia — see AGENTS.md

The architecture + conventions live in [`AGENTS.md`](AGENTS.md) (the vendor-neutral source of
truth, read by every coding agent). Claude Code loads it via the import below.

@AGENTS.md
