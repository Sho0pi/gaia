---
description: Plan a feature before coding — prior art, library choice, test plan.
---

Plan the feature: $ARGUMENTS

1. Restate the goal in one sentence.
2. Delegate to the `lib-researcher` subagent for proven libraries / ADK-native
   options.
3. Find how `google/adk-samples` or a similar agent repo solved this.
4. Propose a design matching that idiom. List the exact files to touch.
5. Define the unit + system tests up front.
6. Do NOT write code yet. Output the plan for approval.
